# Logcat AI

Android Automotive OS(AAOS) 및 일반 Android 기기를 위한 AI 기반 로그 분석 도구입니다.

## 주요 기능
- **실시간 로그 분석**: `adb logcat`을 통한 실시간 에러 감지 및 AI 분석.
- **Git 연동**: 프로젝트 URL을 입력하여 소스 코드 맥락을 파악하고 수정안 제시.
- **AAOS 특화**: VHAL 및 다중 디스플레이 로그 자동 분류 및 모니터링.
- **확장형 대시보드**: 사용자 정의 위젯을 통한 리소스 모니터링.

## 설치 및 실행

### 1. Python 의존성 설치
```bash
pip install -r requirements.txt
```

### 2. OpenCode CLI 설치

OpenCode는 Node.js 기반 CLI 도구입니다. AI 분석 기능을 사용하려면 OpenCode가 필요합니다.

**Node.js 확인** (v18.0.0 이상 필요):
```bash
node --version
npm --version
```

**OpenCode 설치 방법:**

방법 1: 공식 설치 스크립트 (Linux/Mac)
```bash
curl -fsSL https://opencode.ai/install | bash
```

방법 2: npm을 통한 설치
```bash
npm install -g @opencode-ai/cli
```

방법 3: npx 사용 (자동 다운로드)
- 설치 없이 npx를 통해 실행 가능 (코드에서 자동 처리)

**초기 설정:**
```bash
opencode init
# 또는
npx -y @opencode-ai/cli init
```

**API 키 설정** (클라우드 모델 사용 시):
```bash
export ANTHROPIC_API_KEY="sk-ant-..."
export OPENAI_API_KEY="sk-..."
```

### 3. 환경 변수 설정
`.env` 파일을 생성하고 필요한 API 키를 설정하세요.

### 4. 실행
```bash
python src/main.py
```

## OpenCode 사용 방법

1. **이슈 설명 입력**: 상단의 "이슈 설명" 필드에 분석하고 싶은 문제를 입력
2. **분석 요청**: AI Analysis 패널의 "📊 분석 요청" 버튼 클릭
3. **결과 확인**: 분석 결과가 마크다운 형식으로 표시됨
4. **추가 질문**: 하단 채팅 창에서 AI와 대화 가능

**참고**: OpenCode가 프로젝트 코드를 분석하려면 먼저 프로젝트를 로드해야 합니다 (Workspace > Load Project).
